<nav class="navbar navbar-expand-xl navbar-dark fixed-top mb-2">
	<div class="container-fluid">
		<a id="nav-brand" class="navbar-brand text-info" href="{{ path('home') }}">
			<i class="fa fa-bank"></i>
			Digital-Epargne-Service</a>
		<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarCollapse">
			<ul class="navbar-nav mr-auto mb-2 mb-md-0">
				<li class="nav-item m-2">
					<a href="{{ path('person') }}" class="nav-link btn btn-outline-primary text-white">Personne</a>
				</li>
				<li class="nav-item m-2">
					<a href="{{ path('rates') }}" class="nav-link btn btn-outline-primary text-white">Taux</a>
				</li>
				{% if app.user.isAdmin %}
					<li class="nav-item m-2">
						<a href="{{ path('admin') }}" class="nav-link btn btn-outline-primary text-white">Administration</a>
					</li>
				{% endif %}
			</ul>
			<ul class="navbar-nav mr-2 mb-2 mb-md-0">
				<li class="nav-item m-2">
					<a href="{{ path('security_logout') }}" class="nav-link btn btn-outline-danger text-white">Sortir</a>
				</li>
			</ul>
		</div>
	</div>
</nav>
